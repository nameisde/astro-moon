---
const allPosts = Astro.fetchContent('../../pages/projects/**/*.md');
const sortedPosts = allPosts.sort((a, b) => new Date(b.date).valueOf() - new Date(a.date).valueOf());

let read = "🡵 Read Article"
---

<div id="projects-preview">
    <div class="asmo__projects-preview">
        <div class="w-full px-4 lg:px-28 pb-12 lg:pb-20">
            <div class="flex flex-wrap pt-6 gap-6 lg:pt-8 lg:gap-12 mx-auto">
                {sortedPosts.map(({ title, url, category, thumbnail}) =>
                    <div class="w-full lg:flex-auto lg:w-96">
                        <div class="asmo__artile">
                            <a href={url}>
                                <img class=" rounded-lg" src={Astro.resolve(thumbnail)} alt={title}>
                            </a>
                            <div class="py-4">
                                <h4 class="font-bold text-moon-green-900">
                                    {category}
                                </h4>
                                <h2 class="font-clash text-xl font-semibold pt-1 text-moon-black-900">
                                    <a href={url}>{title}</a>
                                </h2>
                                <h4 class="pt-2">
                                    <a href={url} class="font-semibold hover:underline hover:decoration-2 hover:underline-offset-4  hover:decoration-wavy hover:decoration-green-black-900 hover:text-moon-green-900 hover:decoration-moon-green-900">
                                        {read}
                                    </a>
                                </h4>
                            </div>
                        </div>
                    </div>
                )}
            </div>
        </div>
    </div>
</div>